# RELEASE · a v1.0.0 é a primeira MAJOR — uma palavra por conceito, e nada quebra hoje

**de**: ds-diletta `v1.0.0` · **para**: conta-bold-ds · **data**: 2026-09-23

## O que mudou

A linguagem dizia `error` em oito vocabulários e `danger` em dois; `normal` em nove, `neutral` em
três e `neutro` em um. **A mesma coisa com dois nomes** — e a prova mais curta era o badge, cujo
enum dizia `danger` enquanto a resolução ao lado devolvia `s.error`.

| era | é | no seu repo |
|---|---|---|
| `DilettaStatusTone.danger` · `DilettaBadge.danger` | `.error` | **4** sítio(s) |
| `DilettaTextLinkTone.neutro` | `.neutral` | Você não tem nenhum `neutro`. |
| `DilettaRole.danger` | `.error` | zero — saiu sem alias |

**Nada quebra ao subir.** Os três primeiros ficam `@Deprecated` resolvendo IGUAL até a v1.1.0: o
`analyze` aponta cada sítio e nenhum pixel muda enquanto você migra.

**Saíram de vez**, todos com zero chamada real medida nos três filhos: `navGlowDe` (use
`brandSoftDe`) · `footerUpDe` (sem substituto — peça de volta se precisar) · `heroLift` e
`cardLift` (use `brandLowDe`/`brandMediumDe`/`brandHighDe`) · `cardPvDe`.

> Se o seu `analyze` reclamar de algum desses cinco, **a minha medição errou e eu quero o sítio.**
> Eu medi excluindo a cópia do pai que viaja dentro do seu repo — contar ela seria contar o pai
> como consumidor dele mesmo.

## O que você faz

Troque o `ref:` pra `v1.0.0` e rode o `analyze`. Migre os 4 sítio(s) quando quiser, até a
v1.1.0. Tabela completa em `CHANGELOG.md`, entrada `[1.0.0] · O que você faz`; índice de nomes em
`docs/RENOMEACOES.md` do pai.

## Prazo

Os aliases saem na **v1.1.0**. Não há data — há tag.
