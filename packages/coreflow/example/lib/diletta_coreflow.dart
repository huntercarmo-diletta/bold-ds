/// A DILETTA — o Coreflow vestido com a marca da casa. É a CARA do pai, e mora no `example/` dele.
///
/// O pai não declara valor de produto nenhum (é a regra que o gate dele cobra), então a identidade da
/// casa não pode morar no `lib/` do pai — mora aqui, no lugar que o pub reserva pra "como este pacote se
/// veste quando usado". Nasce pela porta de UMA COR: `CoreflowProduto.daMarca` com o `#E60000` do
/// símbolo, e a rampa, os papéis, o vidro e o vocabulário derivam pela régua do avô. O que este pacote
/// declara cabe numa tela: a cor, a marca (dois SVGs) e a fonte. Roda: `flutter run` aqui dentro.
///
/// O Conta BOLD é o primeiro CLIENTE do Coreflow — um filho de paleta inteira, em pacote próprio. Este
/// pacote não o cita, e o gate cobra: é assim que se prova que o pai não sabe que o Bold existe.
library;

export 'package:coreflow/coreflow.dart';
export 'src/diletta.dart';
export 'src/diletta_fundamentos.dart';
export 'src/tela_de_exemplo_diletta.dart';
